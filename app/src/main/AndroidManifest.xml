<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.open_biolab_terminator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenBiolabTerminator">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        // Add other pages in activity so that the app can navigate through the different pages
        // Source : https://www.youtube.com/watch?v=Nq7ZqMAMEfc&amp;ab_channel=AndroidPro => 4:54

        <activity android:name=".Steps"></activity>

    </application>

</manifest>